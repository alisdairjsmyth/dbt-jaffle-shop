snapshots:
  - name: orders_scd2
    relation: ref('stg_orders')
    description:  Snapshot test
    config:
      database: dbt
      schema: dbt_jaffle_shop
      strategy: check
      unique_key: order_id
      check_cols: all
      hard_deletes: ignore